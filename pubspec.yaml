name: web_socket_client
description: Fork of web_socket_client.
version: 0.2.2
topics: [web-socket, web-socket-client]
publish_to: none

environment:
  sdk: "^3.9.0"

dependencies:
  meta: ^1.17.0
  web_socket_channel: ^3.0.3
dev_dependencies:
  mocktail: ^1.0.0
  shelf: ^1.4.2
  shelf_web_socket: ^3.0.0
  test: ^1.19.2
  very_good_analysis: ^10.0.0
